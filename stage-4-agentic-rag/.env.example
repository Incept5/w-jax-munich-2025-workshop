# Stage 4: Agentic RAG - Environment Configuration
# Copy this file to .env and configure as needed

# ==============================================================================
# EMBEDDING PROVIDER CONFIGURATION
# ==============================================================================

# Embedding provider: "python" (default, free, local) or "openai" (paid, simple)
EMBEDDING_PROVIDER=python

# OpenAI API Key (only needed if EMBEDDING_PROVIDER=openai)
# Get your key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-proj-...

# Python Embedding Service URL (only needed if EMBEDDING_PROVIDER=python)
# Default: http://localhost:8001
PYTHON_EMBEDDING_SERVICE_URL=http://localhost:8001

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# PostgreSQL connection (defaults work with docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=workshop_rag
DB_USER=workshop
DB_PASSWORD=workshop

# ==============================================================================
# LLM CONFIGURATION
# ==============================================================================

# Ollama base URL for LLM (not embeddings)
OLLAMA_BASE_URL=http://localhost:11434

# Default LLM model for RAG agent
LLM_MODEL=incept5/Jan-v1-2509:fp16

# ==============================================================================
# COST ESTIMATION (for reference)
# ==============================================================================

# OpenAI Pricing (as of 2024-2025):
# - text-embedding-3-small: $0.00002 per 1k tokens
# - text-embedding-3-large: $0.00013 per 1k tokens
#
# Estimated cost for this workshop (487 documents, ~800 tokens each):
# - Total tokens: ~390k
# - Cost with text-embedding-3-small: ~$0.008 (less than 1 cent)
# - Cost with text-embedding-3-large: ~$0.05 (5 cents)
#
# Python service (nomic-embed-text): FREE (local)
